/*const url_ = "https://apipetshop.herokuapp.com/api/articulos"
let padre = document.getElementById("padre")*/
/**
 * propiedades de los objetos
 * 
 * descripcion
 * imagen
 * nombre
 * precio
 * stock
 * tipo
 * id
 * 
 * myFunction(nData.nombre , padre , "stock" , "precio")
 */

/*fetch(url_)
.then(data => data.json())
.then(nData => myFunction(nData , padre , "id" , "precio") )
.catch(error => alert("error"))


function myFunction(datos , padre , prop1 , prop2){
    datos.array.forEach(element => {
        
    });(element => {
        padre.innerHTML += `
        <h2>${"name:" +" "+ element[prop1]}</h2>
        <h2>${"email:"+" "+ element[prop2]}</h2>
        <hr>  
        `
    })
}*/